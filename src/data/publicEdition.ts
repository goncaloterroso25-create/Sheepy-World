/** Public-copy strings kept together so privacy-sensitive wording cannot drift. */
export const PUBLIC_TOGETHER_COPY = {
  switchSides: 'Switch sides?',
  bump: 'BUMP',
  pinchCallback: 'Got your nose!!!',
} as const;
